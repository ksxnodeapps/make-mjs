/**
 * Title of `docs/index.html`
 */
export const title = 'Make MJS — API Reference'

/**
 * Glob patterns that match package names to be ignored
 */
export const ignoredPackages = [
  'make-mjs',
  'utils'
] as const
